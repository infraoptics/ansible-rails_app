---

- name: make sure the firewalld service is started
  service:
    name: firewalld
    state: started
    enabled: yes
  become: yes
- name: Allow access to http traffic from the proxy
  firewalld:
    zone: internal
    state: enabled
    permanent: yes
    service: "{{ item.0 }}"
    source: "{{ item.1 | default(omit) }}"
  with_together:
  - "{{ rails_app_firewall_services }}"
  - "{{ rails_app_firewall_sources }}"
  notify: reload firewall
